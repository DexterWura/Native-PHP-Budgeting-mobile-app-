{
  "name": "budget-tracker",
  "version": "1.0.0",
  "description": "Budget Tracker Mobile App",
  "scripts": {
    "build:android": "npx cap sync android && npx cap open android",
    "build:ios": "npx cap sync ios && npx cap open ios"
  },
  "keywords": [
    "budget",
    "finance",
    "pwa",
    "mobile"
  ],
  "author": "Dexterity Wurayayi",
  "license": "MIT",
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  }
}

